# 时光信笺字体授权

本模块通过 Fontsource 依赖随 Vite 构建打包以下字体，仅使用 WOFF2 文件：

- Noto Sans SC
- Noto Serif SC
- Ma Shan Zheng

三套字体均按 SIL Open Font License 1.1 发布。完整授权文本随对应的
`@fontsource/*` npm 包提供，路径为各包根目录下的 `LICENSE` 文件。

字体只作为站内卡片排版资源使用，不接受用户上传或替换。

