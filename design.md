<!-- Hallmark · pre-emit critique: P5 H5 E4 S5 R5 V4 -->
<!-- Hallmark · studied: yes · DNA-source: local user-owned homepage · 2026-08-13 -->

# Design — SYSU ISE 2026 迎新站

本文件蒸馏自 Web H5 首页的现有实现与实际渲染。它描述首页应该持续保持的视觉语言，供后续页面、组件与 AI 编码工具复用；不是对全部历史页面样式的汇总。

## System

- 设计母题：校园行动地图（Campus Field Console）
- 气质：年轻、理工、克制、友好；像校园探索工具，不像商业 SaaS 仪表盘
- 界面原则：简洁、重点突出；不使用大面积色块，视觉层级靠排版、字重、留白与细线建立，不靠底色面积
- 结构：短标题引导 + 12 栏非对称 Bento；移动端退化为单列任务流
- 视觉锚点：酸橙路线节点、坐标与网格、浅灰绿卡片群、细线分隔
- 信息优先级：下一步行动 > 个人进度 > 功能入口 > 装饰

## Color

```css
:root {
  --ise-ink:          #0a2e3b; /* 标题、深色主卡、移动导航 */
  --ise-primary:      #0d9488; /* 主要状态、校园人格卡 */
  --ise-primary-dark: #08766d; /* 小标签、次级强调 */
  --ise-accent:       #c7f24a; /* 路线节点、关键 CTA、焦点 */
  --ise-canvas:       #f3f7f5; /* 页面底色 */
  --ise-surface:      #ffffff; /* 普通卡片 */
  --ise-text:         #102a2e; /* 正文主色 */
  --ise-muted:        #5e7271; /* 辅助说明 */
  --ise-border:       #d6e4df; /* 边框与分隔线 */
  --ise-danger:       #c2413a; /* 错误状态 */
}
```

酸橙只占很小面积，用在可行动按钮、路线端点、活动状态和键盘焦点；它不是大面积背景色。深青同样不做大面积铺底：深色页头横幅、整面深色卡片、大面积 tinted 横幅一律禁止；深青只出现在顶部细线、移动导航条、标题文字与小图标。任何颜色都不靠"面积"制造视觉重量。背景以 32px 工程网格建立“地图/蓝图”语境。禁止紫色主视觉、粉紫渐变、低对比灰字和无意义的彩虹配色。

## Typography

### 字体角色

| 角色 | 字体栈 | 用法 |
| --- | --- | --- |
| Display | `"DIN Alternate", "Avenir Next", "Noto Sans SC", "Source Han Sans SC", sans-serif` | 首页 H1、进度数字、年份；紧凑、几何、工程感 |
| Body | `"Noto Sans SC", "Source Han Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif` | 正文、按钮、卡片标题与导航 |
| Technical | `"SFMono-Regular", Menlo, Consolas, monospace` | 坐标、英文 kicker、年份代码、页脚代码 |

### 当前加载事实

- 首页入口当前没有导入任何 `@fontsource` 包，也没有为首页声明 `@font-face`。
- 因此 `DIN Alternate`、`Avenir Next`、`SFMono-Regular` 依赖操作系统；中文通常回退到 `Noto Sans SC`、思源黑体或苹方。
- 仓库虽安装了 Space Grotesk、Noto Sans SC、JetBrains Mono 等字体包，但根目录 `tokens.css` 没有被 Web H5 首页导入，不能视为首页的实际字体来源。
- 若要跨平台严格复现，应显式加载 Noto Sans SC，并为 Display 与 Technical 角色选定可打包字体；在此之前不要声称 DIN 或 SF Mono 一定会呈现。

### 排版规则

- H1：`clamp(35px, 8vw, 72px)`，移动端下限约 30px，行高 `0.98`，700–800 字重。
- 主卡 H2：约 24–39px，行高 `1.1`；功能卡标题约 18–21px，行高 `1.25`。
- 正文：14–16px，行高 `1.55–1.7`，每行说明控制在约 28–40 个中文字符。
- 技术标签：9–11px、粗体、全大写英文；字距保持克制，不做松散的“科技感”拉字。
- 标题保持正体，不使用斜体强调；强调依靠字重、深浅和酸橙色。

## Layout

- 内容最大宽度 1240px；桌面外边距 24–40px，移动端 12–16px。
- Hero 在桌面为 `1.35fr / 0.65fr`：标题左置，说明靠右下对齐；移动端单列。
- 1024px 以上使用 12 栏网格。主探索卡占 7 栏 × 4 行，右侧入口占 5 栏；下方卡片以 5/7 栏交错，避免均匀棋盘。
- 700–1023px 使用两栏；700px 以下所有内容按行动顺序单列。
- 间距遵循 4/8px 节奏：4、8、12、16、24、32、48、64。卡片间距 14–16px。
- 主内容阅读顺序始终是：问候 → 校园探索 → 地图/档案 → 其他活动 → 反馈 → 页脚。

## Surfaces and components

- 普通卡片：1px 边框、24px 圆角、平坦浅色表面；仅 hover/focus 或浮层使用轻阴影。
- 主任务卡：同样是浅色表面，靠更大的版面占比、Display 字号与酸橙 CTA 成为视觉锚点，不铺深色底；地图网格、坐标与路线轮廓以低透明度浅色线条叠加。
- 图标砖：约 38–46px，采用 `14px 5px 14px 5px` 非对称圆角；图标统一使用 Lucide Vue。
- CTA：主要操作使用酸橙胶囊，深青文字；次级操作为文字/图标组合。点击目标至少 44×44px。每屏只保留一个主行动，重复入口直接删除。
- 状态 chip：白或浅灰绿底、细边框、小字号粗体；只表达真实状态，不把未开放功能伪装成可用。
- Desktop 导航位于顶部半透明浅色容器；Mobile 导航固定底部，深青底、四等分，当前项用酸橙标记。
- 欢迎弹窗沿用浅色表面 + 细边框，配酸橙 CTA、坐标、同心路线弧与错角圆角；不使用深青大面。

## Motion

- 首屏标题采用分行上移揭示，约 620ms；说明与卡片用 220–480ms 淡入上移，卡片错峰约 40ms。
- 进度路线用 `scaleX` 绘制；地图网格、路线节点只做低幅、低频呼吸或漂移。
- 卡片 hover 上移不超过 3px；active 仅轻微缩至 `0.99`，不移动周围布局。
- 动效只改变 `transform` 与 `opacity`。使用强 ease-out，如 `cubic-bezier(.16, 1, .3, 1)`。
- 在 `prefers-reduced-motion: reduce` 下移除入场、循环、路线与 hover 动效，内容直接呈现。

## Responsive behavior

- Mobile-first；375px 是核心基准，底部为固定四项导航，并为安全区预留页面底部空间。
- 480px 以下缩小标题、图标砖和卡片内边距，但保留 44px 触控面积。
- 700px 开始两栏；1024px 切换为桌面导航和 12 栏非对称 Bento。
- 所有图片与网格轨道使用可收缩尺寸；正文和标题不得造成横向滚动。

## Accessibility

- 普通文字对比度至少 4.5:1，大字和图标至少 3:1。
- 链接、按钮保留可见 `:focus-visible`；焦点使用酸橙外圈与深青辅助环。
- 图标按钮必须有描述性名称；键盘顺序跟随视觉顺序。
- 弹窗需要焦点圈闭、Escape 关闭，并在关闭后把焦点交回主要内容。
- Loading 保留版面高度；错误状态必须同时给出原因与“重试”恢复动作。
- 重要含义不能只靠颜色、hover 或动画表达。

## Content voice

- 中文先说行动与场景，英文只作为技术标签和校园坐标注释。
- 用“第一站、下一站、坐标、点亮、路线、出发”维持探索叙事。
- 文案亲切但不卖萌；避免商业营销口号、虚构指标和泛化的“赋能/生态/一站式”。
- 每张功能卡只回答三件事：这是什么、为什么要去、现在能做什么。

## Guardrails

- 不使用大色块：深色页头横幅、整面深色卡片、大面积 tinted 提示条、大面积渐变底一律禁止。
- 保持简洁、重点突出：每屏一个主行动；重复的入口、横幅与装饰性文案直接删除，不为"填满版面"加元素。
- 不把首页改造成标准 hero + 三功能 + CTA 的营销模板。
- 不平均分配卡片尺寸；主探索任务保持最高的信息层级与版面占比，但用尺寸与排版突出，不靠大面积深色。
- 不新增玻璃拟态大面、厚重阴影、装饰性无限动画或 emoji 结构图标。
- 不让酸橙扩张成背景洪水；不让网格盖过真实信息。
- 后续页面可以改变卡片组合，但必须复用颜色角色、字体角色、4/8px 节奏与路线/坐标语汇。

## Provenance

- Source：`apps/web-h5/src/pages/Home.vue` 的模板、作用域样式与本地实际渲染。
- Supporting reference：`design-system/sysu-ise-2026-welcome/MASTER.md`。
- Extracted：2026-08-13，用户自有本地项目。
- Revised：2026-08-14，确立"不要大色块、界面简洁重点突出"原则；删除深青主任务面板、深青大面弹窗等与之矛盾的描述。首页与欢迎弹窗中现存的深青大面属于待改造的遗留实现，不再作为范式。
- Confidence：颜色、布局与运动为代码精确值；视觉节奏经桌面与 375px 渲染核对；字体名称为声明值，但部分字体的实际呈现依赖操作系统回退。
